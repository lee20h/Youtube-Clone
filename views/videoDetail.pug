extends layout/main

block content
    .video__player
        video(src=`/${video.fileUrl}`, controls=true)
    .video__info
        h5.video__title=video.title
        span.video__views=video.views
        p.viode__description=video.description